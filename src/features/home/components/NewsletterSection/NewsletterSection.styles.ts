// Centered newsletter band: title + subheading over an inline email field and submit button.
export const newsletterRootClass = 'mx-auto flex max-w-2xl flex-col items-center gap-5 text-center';

export const newsletterFormClass = 'flex w-full flex-col gap-3 sm:flex-row sm:items-end';

export const newsletterInputClass = 'sm:flex-1';
