import { cva } from 'class-variance-authority';

// Stacks the breadcrumb trail, the page heading, and the cart body with a comfortable rhythm.
export const cartPageRootClass = 'flex flex-col gap-6';

// The "Количка" page heading row.
export const cartHeadingClass = 'mt-2';
