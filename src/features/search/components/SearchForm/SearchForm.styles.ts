// Search field + submit button laid out on one row, matching the Liquid `input-group--nowrap`.
export const searchFormClass = 'flex w-full max-w-2xl items-start gap-2';

// The field flexes to fill the row; the Input primitive renders its own column wrapper inside.
export const searchFieldClass = 'flex-1';

export const searchSubmitClass = 'shrink-0';
