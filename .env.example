# Public site origin (used for canonical URLs, sitemap, OG).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Backend API base URL — the storefront backend lives in a SEPARATE repo/service.
# The frontend reads catalog/cart/etc. over HTTP from here (server-side).
BACKEND_API_URL=http://localhost:4000

# Product media host (object storage / CDN), once chosen. Drives next/image remotePatterns.
NEXT_PUBLIC_MEDIA_HOST=
